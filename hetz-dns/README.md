# Only Hetzner DNS
This only creates DNS records in Hetzner-DNS.
Auth to hcloud  used shell variable HCLOUD_TOKEN.

## State files saved on local minio
Authentication to minio using shell variabales
AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
